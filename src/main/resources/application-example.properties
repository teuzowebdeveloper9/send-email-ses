spring.application.name=tecnical

# AWS credentials and region for SES (use env vars or a credentials profile in production)
aws.accessKeyId=YOUR_AWS_ACCESS_KEY_ID
aws.secretKey=YOUR_AWS_SECRET_ACCESS_KEY
aws.region=us-east-1

# Verified SES sender address
ses.fromEmail=verified-sender@example.com

